你现在最应该做的不是“重建一个科技调研项目”，而是把现有工作区升级成：

> **通用板块调研引擎 + 本次科技主线调研实例**

也就是说，`investment_system` 是可复用系统；`科技主线调研输出` 只是一次运行结果；以后你调研“化工新材料”“低空经济”“游戏传媒”“军工电子”，都应该复用同一套 engine、schema、pipeline、quality gate。

---

# 1. 先明确你的目标架构

你现在已有结构其实已经不错，问题是有些地方还带有“科技主线调研”的一次性痕迹。

建议目标架构如下：

```text
C:\Projects\03_Investment
│
├─ investment_system/                 # 通用投研系统，不绑定具体板块
│  ├─ config/
│  ├─ docs/
│  ├─ data/
│  ├─ research/
│  │  ├─ evidence/                    # 公司/行业证据沉淀
│  │  ├─ projects/                    # 每次板块调研实例
│  │  │  └─ tech_ai_semiconductor/    # 本次科技主线调研只是一个实例
│  │  │     ├─ project.yaml
│  │  │     ├─ sector_universe.yaml
│  │  │     ├─ stock_universe.yaml
│  │  │     ├─ scoring_rules.yaml
│  │  │     ├─ output_spec.yaml
│  │  │     └─ run_manifest.yaml
│  │  ├─ templates/
│  │  │  ├─ sector_card_template.md
│  │  │  ├─ comparison_table_schema.yaml
│  │  │  └─ source_index_schema.yaml
│  │  └─ schemas/
│  │     ├─ project.schema.yaml
│  │     ├─ sector_universe.schema.yaml
│  │     ├─ evidence.schema.yaml
│  │     └─ scoring.schema.yaml
│  │
│  ├─ pipelines/
│  │  ├─ sector_research/
│  │  │  ├─ load_project.py
│  │  │  ├─ build_sector_universe.py
│  │  │  ├─ build_stock_universe.py
│  │  │  ├─ collect_market_data.py
│  │  │  ├─ collect_financial_data.py
│  │  │  ├─ merge_evidence.py
│  │  │  ├─ score_sectors.py
│  │  │  ├─ generate_sector_cards.py
│  │  │  ├─ generate_comparison_tables.py
│  │  │  └─ audit_outputs.py
│  │  └─ ...
│  │
│  └─ scripts/
│
├─ 科技主线调研输出/                  # 本次调研输出，可保留
│
├─ .codex/
│  ├─ skills/
│  └─ agents/                         # 可选，后面再做
│
└─ .conda/
```

重点变化只有一个：

> **把“科技主线”从系统逻辑里剥离出来，放到 `research/projects/tech_ai_semiconductor/` 作为配置。**

---

# 2. 不要马上搬文件，先做“配置化”

你的当前输出目录可以先不动。第一阶段只做配置化，让系统知道：

```yaml
project_id: tech_ai_semiconductor
project_name: A股科技主线调研
output_root: C:/Projects/03_Investment/科技主线调研输出
```

这样既不破坏已有成果，又能把这次科技调研变成一个可复用实例。

建议新增：

```text
investment_system/research/projects/tech_ai_semiconductor/project.yaml
```

内容类似：

```yaml
project_id: tech_ai_semiconductor
project_name: A股科技主线调研
research_type: sector_research
market: A股
scope:
  include:
    - AI算力
    - 半导体
    - 光通信
    - PCB
    - 先进封装
    - 存储
    - AI终端
    - AI应用
  exclude:
    - 机器人

output:
  root: C:/Projects/03_Investment/科技主线调研输出
  sector_cards_dir: 细分方向报告
  total_tables_dir: 00_总表
  logs_dir: 99_日志

runtime:
  python: C:/Projects/03_Investment/.conda/investment-system/python.exe

quality:
  grade_target: research-grade
  require_sources: true
  allow_placeholders: false
  stale_days:
    market_data: 3
    financial_data: 120
    news_or_announcements: 30
```

以后换成“化工新材料调研”，只需要复制一个：

```text
investment_system/research/projects/chemical_materials/project.yaml
```

而不是重写 pipeline。

---

# 3. 当前工作区最需要改的 6 个点

## 3.1 把 `科技主线调研输出` 降级为 output，不再作为输入本体

现在 `research_workflow.md` 还提到 `A股科技前两主线调研文件包`，这说明你的系统文档里有历史痕迹。

应该改成：

```text
板块调研输入来自：
1. research/projects/<project_id>/project.yaml
2. research/projects/<project_id>/sector_universe.yaml
3. research/projects/<project_id>/stock_universe.yaml
4. research/evidence/*.yaml
5. data/raw 与 data/processed

板块调研输出写入：
project.yaml 中定义的 output.root
```

也就是说，输出目录不要再作为“体系入口”。

---

## 3.2 增加 `research/projects/` 作为“调研实例层”

你现在有 `research/evidence/*.yaml`，很好。但还缺一个层级来表达：

- 这次研究什么板块？
    
- 包含哪些细分方向？
    
- 每个方向有哪些代表公司？
    
- 评分权重是什么？
    
- 输出到哪里？
    
- 哪些数据缺失可以容忍？
    
- 哪些字段必须强制验证？
    

所以建议新增：

```text
investment_system/research/projects/
```

每个项目一个目录：

```text
investment_system/research/projects/tech_ai_semiconductor/
investment_system/research/projects/chemical_materials/
investment_system/research/projects/game_media/
investment_system/research/projects/defense_electronics/
```

---

## 3.3 把“板块 universe”和“股票 universe”分开

你现在的 `config/universe.example.yaml` 偏股票池、评分权重、风控规则样例。对于板块调研，最好拆成两个文件。

### `sector_universe.yaml`

管“研究哪些细分方向”：

```yaml
sectors:
  - sector_id: cpo_optical_module
    sector_name: CPO/高速光模块
    parent_chain: AI算力硬件/光通信
    priority: P0
    include_concepts:
      - CPO
      - 800G光模块
      - 1.6T光模块
      - 硅光
      - LPO
    exclude_concepts: []
    research_questions:
      - AI数据中心需求是否继续上修？
      - 800G向1.6T切换是否带来ASP提升？
      - 龙头估值是否已透支2027年？
    required_outputs:
      - sector_card
      - company_comparison
      - score
      - source_index
```

### `stock_universe.yaml`

管“每个方向有哪些股票”：

```yaml
stocks:
  - code: 300308.SZ
    name: 中际旭创
    sectors:
      - cpo_optical_module
    role: 龙头
    exposure_type: 核心收入
    notes: 高速光模块核心标的

  - code: 300502.SZ
    name: 新易盛
    sectors:
      - cpo_optical_module
    role: 龙头
    exposure_type: 核心收入

  - code: 300394.SZ
    name: 天孚通信
    sectors:
      - cpo_optical_module
      - optical_components
    role: 上游核心
    exposure_type: 高纯度
```

这样之后比较 PCB、CPO、半导体设备时，不会把“方向”和“股票”混在一起。

---

## 3.4 把评分规则从文档中抽出来

你现在应该有报告质量标准，但“建仓适合度”需要成为机器可读配置。

新增：

```text
investment_system/research/projects/tech_ai_semiconductor/scoring_rules.yaml
```

示例：

```yaml
score_model: sector_build_position_score
version: 1

dimensions:
  prosperity:
    name: 景气度
    weight: 0.20
    description: 产业需求、订单、价格、资本开支、行业验证
  earnings_certainty:
    name: 业绩确定性
    weight: 0.20
    description: 收入利润兑现、毛利率、预测上修、客户订单
  valuation:
    name: 估值性价比
    weight: 0.20
    description: PE、PEG、PS、历史分位和成长匹配度
  trading_comfort:
    name: 筹码舒适度
    weight: 0.15
    description: 涨幅、换手、成交额、均线偏离、拥挤度
  catalyst:
    name: 催化剂强度
    weight: 0.10
    description: 未来1-6个月产业/政策/业绩/事件催化
  purity:
    name: 产业纯度
    weight: 0.10
    description: 主营相关度、收入占比、客户验证
  risk_control:
    name: 风险可控性
    weight: 0.05
    description: 估值、供给、技术路线、客户集中和业绩证伪风险

ratings:
  A:
    name: 优先建仓
    min_total_score: 7.5
    required:
      prosperity: 8
      earnings_certainty: 7
      valuation: 6
      trading_comfort: 6
  B:
    name: 逻辑强但等回调
    min_total_score: 7.0
  C:
    name: 低位但需验证
    min_total_score: 6.0
  D:
    name: 只买龙头
    min_total_score: 5.5
  E:
    name: 暂不参与
    min_total_score: 0
```

---

## 3.5 让 pipeline 以 `project_id` 为唯一入口

你最终希望这样运行：

```powershell
& "C:\Projects\03_Investment\.conda\investment-system\python.exe" `
  -m investment_system.pipelines.sector_research.run `
  --project tech_ai_semiconductor
```

而不是：

```powershell
python collect_high_speed_optical.py
python build_科技主线总表.py
python generate_光模块报告.py
```

也就是说，pipeline 不应该写死：

- 科技主线
    
- 光模块
    
- PCB
    
- 某个输出路径
    
- 某个 CSV 文件名
    

它应该只认识：

```text
project_id
sector_id
stock_code
output_root
```

---

## 3.6 把 `tools/collect_high_speed_optical.py` 收编

你现在 `tools` 里只有一个临时采集脚本，名字已经绑定“高速光模块”。

建议不要删，先迁移成：

```text
investment_system/pipelines/sector_research/collectors/optical_module.py
```

或者更通用：

```text
investment_system/pipelines/sector_research/collectors/concept_market_collector.py
```

保留旧文件，但加说明：

```text
tools/collect_high_speed_optical.py
```

可改成 wrapper：

```python
"""
Deprecated wrapper.
Please use:
python -m investment_system.pipelines.sector_research.collectors.concept_market_collector --project tech_ai_semiconductor --sector cpo_optical_module
"""
```

这样不破坏历史调用。

---

# 4. Skills 应该怎么改

你现有七个 skills 方向是对的，不需要推倒。

但要改它们的职责边界。

## 4.1 `sector-research-orchestrator`

现在它服务“A股科技主线/板块深度调研”。应该改成：

> 接收 `project_id` 和可选 `sector_id`，读取 `research/projects/<project_id>/`，按工作流调度其他 skills。

不要在 skill 里写死：

```text
科技主线调研输出
高速光模块
CPO
PCB
```

而是写：

```text
project.output.root
project.sector_universe
project.stock_universe
```

## 4.2 `market-data-router`

保持通用，不要改成科技专用。

只要求它支持：

```text
输入：stock_universe.yaml
输出：market_snapshot.csv / market_snapshot.yaml
```

## 4.3 `financial-data-router`

保持通用。

需要新增一个要求：

```text
所有财务字段必须带 fiscal_period 和 source_id。
```

否则后面总表会发生“TTM、2025A、2026E 混用”。

## 4.4 `evidence-miner`

这是最关键的 skill。

它应该沉淀证据，而不是直接写最终报告。

证据 YAML 建议统一：

```yaml
evidence_id: ev_20260620_0001
subject_type: company
subject_id: 300308.SZ
subject_name: 中际旭创
sector_id: cpo_optical_module
claim: 公司高速光模块收入受AI数据中心需求驱动增长
evidence_type: annual_report
source:
  source_id: src_20260620_0001
  title: 2025年年度报告
  date: 2026-04-20
  url_or_path: ...
confidence: high
fields_supported:
  - revenue
  - ai_exposure
  - customer_demand
notes: ...
```

## 4.5 `research-writer`

它只负责从结构化数据生成：

- sector card
    
- 总表
    
- 来源索引
    
- 缺失数据清单
    
- 冲突数据清单
    

不要让它临时发挥观点。观点必须来自 scoring 和 evidence。

## 4.6 `quality-auditor`

建议新增检查项：

```text
1. 是否存在科技主线硬编码路径
2. 是否存在无 source_id 的关键结论
3. 是否存在评分无理由
4. 是否存在财务年份混用
5. 是否存在输出表字段错位
6. 是否存在旧项目名残留
7. 是否存在只看概念分类、没有收入/订单/客户验证的板块结论
```

---

# 5. 你应该让 Codex 先做“审计”，不要直接让它改

Codex CLI 的官方说明是，它可以在你选定的目录里读代码、改代码并运行代码；所以第一步一定要限制它先审计、出方案，不要直接重构。([开放AI开发者](https://developers.openai.com/codex/cli?utm_source=chatgpt.com "Codex CLI"))

你可以先给 Codex 这个任务：

请先审计当前工作区，不要修改任何文件。

工作区根目录：  
C:\Projects\03_Investment

目标：  
我想把当前 A股科技主线调研项目，升级为可复用的“板块调研工作区”。科技主线调研只是第一个实践项目，未来还要复用到化工、游戏、军工、消费电子等板块。

请完成以下审计：

1. 扫描 investment_system、.codex、tools、科技主线调研输出 的结构。
    
2. 找出当前哪些文件或脚本写死了“科技主线”“高速光模块”“A股科技前两主线调研文件包”“科技主线调研输出”等一次性项目痕迹。
    
3. 找出哪些模块已经足够通用，不应该重写。
    
4. 给出最小改造方案，原则：
    
    - 不移动、不删除现有成果文件。
        
    - 不破坏已有 pipeline。
        
    - 优先新增 project 配置层，而不是重写逻辑。
        
    - 输出目录先继续使用 C:\Projects\03_Investment\科技主线调研输出。
        
    - 所有新增文件必须说明用途。
        
5. 最后输出：
    
    - 需要新增的文件清单
        
    - 需要修改的文件清单
        
    - 不建议修改的文件清单
        
    - 分阶段实施计划
        
    - 风险点
        

只审计，不改文件。

---

# 6. 第二步再让 Codex 做“最小骨架改造”

等它审计完，你再让它进入第一阶段改造。

请基于上一步审计结果，执行第一阶段最小骨架改造。

目标：  
在不移动、不删除现有文件的前提下，把当前工作区升级为“通用板块调研引擎 + 科技主线调研实例”。

请执行：

1. 新增目录：
    
    - investment_system/research/projects/
        
    - investment_system/research/projects/tech_ai_semiconductor/
        
    - investment_system/research/templates/
        
    - investment_system/research/schemas/
        
2. 新增文件：
    
    - investment_system/research/projects/tech_ai_semiconductor/project.yaml
        
    - investment_system/research/projects/tech_ai_semiconductor/sector_universe.yaml
        
    - investment_system/research/projects/tech_ai_semiconductor/stock_universe.yaml
        
    - investment_system/research/projects/tech_ai_semiconductor/scoring_rules.yaml
        
    - investment_system/research/projects/tech_ai_semiconductor/output_spec.yaml
        
    - investment_system/research/projects/tech_ai_semiconductor/run_manifest.yaml
        
    - investment_system/research/templates/sector_card_template.md
        
    - investment_system/research/templates/comparison_table_schema.yaml
        
    - investment_system/research/templates/source_index_schema.yaml
        
    - investment_system/research/schemas/project.schema.yaml
        
    - investment_system/research/schemas/sector_universe.schema.yaml
        
    - investment_system/research/schemas/scoring.schema.yaml
        
3. project.yaml 中明确：
    
    - project_id: tech_ai_semiconductor
        
    - project_name: A股科技主线调研
        
    - output.root: C:/Projects/03_Investment/科技主线调研输出
        
    - runtime.python: C:/Projects/03_Investment/.conda/investment-system/python.exe
        
    - exclude: 机器人
        
4. sector_universe.yaml 先录入当前科技主线细分方向，但不要过度扩写报告内容。
    
5. stock_universe.yaml 先录入已有报告和总表中已经出现的代表公司，标记来源为 migrated_from_existing_outputs。
    
6. scoring_rules.yaml 使用：
    
    - 景气度 20%
        
    - 业绩确定性 20%
        
    - 估值性价比 20%
        
    - 筹码舒适度 15%
        
    - 催化剂强度 10%
        
    - 产业纯度 10%
        
    - 风险可控性 5%
        
7. 不要修改已有 pipeline 逻辑。
    
8. 不要改已有调研输出内容。
    
9. 完成后输出 git diff 风格摘要，说明新增了哪些文件、每个文件的作用，以及下一步应该改哪个 pipeline。
    

---

# 7. 第三步：加一个 project loader

这一步才开始改代码。

你需要一个通用入口：

```text
investment_system/pipelines/sector_research/load_project.py
```

它做的事情很简单：

```python
load_project(project_id)
  -> project.yaml
  -> sector_universe.yaml
  -> stock_universe.yaml
  -> scoring_rules.yaml
  -> output_spec.yaml
```

然后所有 pipeline 后面都用它。

你可以让 Codex 实现：

```text
python -m investment_system.pipelines.sector_research.load_project --project tech_ai_semiconductor
```

输出：

```json
{
  "project_id": "tech_ai_semiconductor",
  "project_name": "A股科技主线调研",
  "sector_count": 20,
  "stock_count": 120,
  "output_root": "C:/Projects/03_Investment/科技主线调研输出",
  "scoring_model": "sector_build_position_score"
}
```

这一步的意义很大：从此所有调研都变成 `--project xxx`。

---

# 8. 第四步：把现有输出反向登记为 evidence/source

你现在已经有：

```text
代表公司财务估值总表.csv
科技细分方向横向比较表.csv
数据来源索引.csv
高速光模块.md
光器件_FAU_精密光学.md
```

不要让 Codex 重写它们。应该先让 Codex 做“迁移索引”：

```text
investment_system/research/projects/tech_ai_semiconductor/run_manifest.yaml
```

记录当前已有成果：

```yaml
existing_outputs:
  - path: C:/Projects/03_Investment/科技主线调研输出/00_总表/代表公司财务估值总表.csv
    type: company_financial_valuation_table
    status: existing
    action: index_only

  - path: C:/Projects/03_Investment/科技主线调研输出/00_总表/科技细分方向横向比较表.csv
    type: sector_comparison_table
    status: existing
    action: index_only

  - path: C:/Projects/03_Investment/科技主线调研输出/01_AI算力硬件/01_高速光模块.md
    type: sector_card
    sector_id: cpo_optical_module
    status: existing
    action: index_only
```

这样你不会丢历史成果，也不会让新系统误以为这些是手写散文。

---

# 9. 第五步：再把 pipeline 改成 project-aware

等配置层和 loader 稳定后，才改 pipeline。

优先级如下：

## 9.1 先改 writer，不改采集器

原因：writer 最容易改，风险最低。

让 `research-writer` / 相关 pipeline 支持：

```text
--project tech_ai_semiconductor
--sector cpo_optical_module
```

读取：

```text
research/projects/tech_ai_semiconductor/
```

写入：

```text
project.yaml -> output.root
```

## 9.2 再改 quality-auditor

因为你要马上防止错误扩散。

让它检查：

```text
python -m investment_system.pipelines.sector_research.audit_outputs --project tech_ai_semiconductor
```

输出：

```text
缺失数据清单.md
冲突数据清单.md
占位符检查
来源检查
评分理由检查
路径硬编码检查
```

## 9.3 最后改数据采集器

市场、财务、证据、预测采集器可以最后改。因为这块对数据源依赖高，容易出问题。

---

# 10. 你需要一个顶层 `AGENTS.md`

如果你用 Codex，建议在：

```text
C:\Projects\03_Investment\AGENTS.md
```

放项目级规则。官方 Codex 文档说明，Codex 会在开始工作前读取 `AGENTS.md`，可以用它做项目级持久指令；`/init` 也可以生成 AGENTS.md 初始骨架。([开放AI开发者](https://developers.openai.com/codex/guides/agents-md?utm_source=chatgpt.com "Custom instructions with AGENTS.md – Codex"))

你的 `AGENTS.md` 应该写这些硬约束：

```markdown
# Project Instructions for Codex

## Project
This is an A-share investment research workspace.

## Hard constraints
- Do not move or delete existing research outputs.
- Do not overwrite files under 科技主线调研输出 unless explicitly asked.
- Use project runtime:
  C:\Projects\03_Investment\.conda\investment-system\python.exe
- Treat investment_system as the reusable research engine.
- Treat research/projects/<project_id> as research project instances.
- Treat 科技主线调研输出 as output artifacts, not system source code.
- Prefer adding config/project layers before rewriting pipelines.
- Every research conclusion must have source_id or a missing-data entry.
- Do not hard-code 科技主线, 高速光模块, CPO, PCB, or output paths in generic pipeline code.

## Workflow
1. Inspect existing docs and pipeline before modifying.
2. Propose file-level changes.
3. Make small patches.
4. Run validation or explain why validation cannot run.
5. Summarize modified files and risks.
```

你已有 `.codex/skills`，这很好。Codex 官方文档也说明 skills 会按需加载完整指令，所以 skill 可以继续承担“具体任务说明”，而 `AGENTS.md` 负责全局不可违反的工程规则。([开放AI开发者](https://developers.openai.com/codex/skills?utm_source=chatgpt.com "Agent Skills – Codex"))

---

# 11. `.agents` 目录暂时不用管

你提到：

```text
C:\Projects\03_Investment\.agents
```

存在但没有文件。

这个阶段不要急着做多 agent。先把单 project pipeline 跑通。

后面如果要加 Codex 子代理，可以用项目级：

```text
.codex/agents/
```

官方 Codex subagents 文档说明，项目级自定义 agent 可以放在 `.codex/agents/` 下。([开放AI开发者](https://developers.openai.com/codex/subagents?utm_source=chatgpt.com "Subagents – Codex"))

但我建议顺序是：

1. 先有 project config
    
2. 再有 project loader
    
3. 再有 writer/auditor
    
4. 最后才拆 subagents
    

否则你会得到很多 agent，但没有统一数据结构。

---

# 12. 你这次科技调研应该如何落地

对于“当前科技主线调研”，不要重新开始。用它做第一个标准样板。

建议把现有成果映射成：

|当前文件|新系统身份|
|---|---|
|`科技细分方向横向比较表.csv`|`sector_comparison_table`|
|`代表公司财务估值总表.csv`|`company_financial_valuation_table`|
|`数据来源索引.csv`|`source_index`|
|`01_高速光模块.md`|`sector_card: cpo_optical_module`|
|`01_光器件_FAU_精密光学.md`|`sector_card: optical_components`|
|`缺失数据清单.md`|`missing_data_log`|
|`冲突数据清单.md`|`conflict_data_log`|
|`调研日志.md`|`research_run_log`|

然后新增尚未完成的 sector cards：

```text
02_AI服务器PCB.md
03_高频高速CCL.md
04_高速铜缆_连接器.md
05_AI服务器_整机柜.md
06_液冷_温控.md
07_数据中心电源.md
08_半导体设备.md
09_半导体材料.md
10_存储_HBM.md
11_先进封装.md
12_国产AI芯片.md
13_端侧AI.md
14_AI应用_数据要素.md
```

但这些文件不应该手工散写，而应该由 `research-writer` 按模板生成。

---

# 13. 最小可运行版本，做到这里就够了

你的第一阶段目标不是“一次性全自动”，而是做到：

```powershell
& "C:\Projects\03_Investment\.conda\investment-system\python.exe" `
  -m investment_system.pipelines.sector_research.load_project `
  --project tech_ai_semiconductor
```

能读出：

```text
项目：A股科技主线调研
细分方向：20 个
股票池：若干
输出目录：C:\Projects\03_Investment\科技主线调研输出
评分模型：sector_build_position_score
质量等级：research-grade
```

然后做到：

```powershell
& "C:\Projects\03_Investment\.conda\investment-system\python.exe" `
  -m investment_system.pipelines.sector_research.audit_outputs `
  --project tech_ai_semiconductor
```

能检查已有输出。

这就是最小可用系统。

不要一开始就要求 Codex：

```text
帮我把所有细分方向调研完。
```

你应该先要求它：

```text
帮我让这个工作区支持 project_id 驱动的板块调研。
```

---

# 14. 最推荐的实施顺序

## 阶段 0：审计，不改文件

目标：找到硬编码和历史痕迹。

产物：

```text
docs/refactor_audit_sector_workspace.md
```

## 阶段 1：新增项目配置层

目标：让科技调研成为第一个 project instance。

产物：

```text
research/projects/tech_ai_semiconductor/*.yaml
research/templates/*.md
research/schemas/*.yaml
```

## 阶段 2：新增 project loader

目标：让 pipeline 可以通过 `--project` 读取配置。

产物：

```text
pipelines/sector_research/load_project.py
```

## 阶段 3：改 writer 和 auditor

目标：先让输出生成和质量检查 project-aware。

产物：

```text
generate_sector_cards.py
generate_comparison_tables.py
audit_outputs.py
```

## 阶段 4：迁移已有科技调研成果为索引

目标：保留旧成果，同时纳入新体系。

产物：

```text
run_manifest.yaml
source_index normalized
missing/conflict logs normalized
```

## 阶段 5：再逐个接入采集器

目标：行情、财务、证据、预测全部 project-aware。

产物：

```text
collect_market_data.py
collect_financial_data.py
merge_evidence.py
normalize_forecast.py
```

---

# 15. 一句话判断

你的系统不需要重建。  
你现在缺的是一层：

> **`research/projects/<project_id>/` 项目配置层 + `--project` 驱动的 pipeline 入口。**

先让 Codex 做审计，再做最小骨架，再做 project loader。等这三步完成后，“科技主线调研”就会从一次性任务变成第一个可复用样板。